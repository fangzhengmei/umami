# Umami 共享链接访问控制机制分析

## 1. 共享链接的生成、存储与校验流程

### 1.1 生成
- 共享链接由服务端接口创建，核心入口在 `src/app/api/websites/[websiteId]/shares/route.ts` 与 `src/app/api/share/route.ts`。
- 创建时会生成随机 `slug`（用于公开访问 URL）和 token，并绑定实体类型（如网站、看板）与实体 ID。
- 生成动作要求调用方具备对应资源的管理能力，避免普通访客自行创建共享入口。

### 1.2 存储
- 共享配置持久化在 Share 模型（见 `prisma/schema.prisma` 与 `src/queries/prisma/share.ts`）。
- 记录包含实体维度、slug、token 与必要元数据，供后续访问链路反查权限上下文。
- 数据层以 `slug` / `entityId` 等键支持快速定位，保证访问校验链路可在接口层快速完成。

### 1.3 校验
- 访客访问共享链接时，服务端先按 `slug` 查询共享记录（入口见 `src/app/api/share/[slug]/route.ts`）。
- 通过共享 token + 共享上下文头（如 `x-umami-share-token`、`x-umami-share-context`）建立“共享访客身份”。
- 认证层将该身份注入请求上下文，后续权限函数以统一入口做判定（核心在 `src/lib/auth.ts` 与 `src/permissions/website.ts`）。

## 2. 共享访客访问时的权限判定路径

1. 客户端进入共享页面（`src/app/share/[slug]/[[...path]]/SharePage.tsx`）。
2. 前端通过 ShareProvider / hook 写入共享 token 与上下文头（`src/app/share/ShareProvider.tsx`、`src/components/hooks/context/useShare.ts`）。
3. 业务 API 在 `parseRequest` 后进入资源权限校验。
4. `canViewWebsite` 等权限函数按“管理员/成员身份 -> 共享身份 -> 归属关系”路径判定是否可读。
5. 判定失败返回未授权；判定成功才进入统计、会话、事件等查询逻辑。

结论：共享访客不会绕过标准 API 权限闸门，而是作为一种“受限可读身份”进入同一判定流程。

## 3. 团队成员权限与共享访客权限边界

### 3.1 团队成员
- 通过账号体系与组织关系授权，可按角色获得完整资源访问能力。
- 具备成员维度的操作权限（取决于角色），不仅限于读取。

### 3.2 共享访客
- 不依赖团队账号，依赖共享 token + 上下文头建立访问身份。
- 权限边界是“仅共享范围内的读取能力”，不具备团队成员管理能力与写入能力。
- 一旦离开共享上下文，请求将失去共享身份，不会获得额外权限。

结论：团队成员与共享访客的核心差异不在查询接口形态，而在身份来源、操作能力和越权边界。

## 4. 查询数据在权限裁剪后的返回口径

- 查询链路先过权限，再进入数据层：只有已通过 `canViewWebsite` 的请求会触发统计查询。
- 对共享访客而言，权限裁剪发生在“是否允许读取该网站”这一层，而不是在结果字段上做额外删减。
- 因而在同一网站、同一查询条件下，共享访客与有读权限成员通常获得同口径数据结果。
- 仍会受全局数据限制影响（如站点重置时间、部署模式下的数据时间窗策略），但这些限制是资源级通用规则，不是共享身份特例。

## 5. 总结

- Umami 共享访问采用“共享身份注入 + 统一权限闸门”的设计。
- 共享链接生命周期完整覆盖了创建、持久化、身份校验与接口保护。
- 权限模型把“是否可读”收敛到统一函数，避免共享链路成为旁路。
- 团队成员与共享访客在查询结果口径上趋同，在操作边界与身份可信度上严格区分。
