# 报表导出实现流程分析

## 一、导出完整流程

报表导出功能涉及前端用户操作、后端权限验证、数据查询组装、文件生成和下载五个核心阶段。

### 1.1 流程总览

```
用户点击导出按钮
        ↓
[前端] ExportButton.tsx - 发起API请求
        ↓
[API路由] route.ts - 接收请求，参数验证
        ↓
[权限层] parseRequest + canViewWebsite - 身份认证与权限校验
        ↓
[查询组装] getQueryFilters - 构建查询过滤条件
        ↓
[数据查询] 7类Metrics并行查询 (Promise.all)
        ↓
[文件生成] JSZip打包 + PapaParse生成CSV
        ↓
[响应返回] Base64编码的ZIP文件
        ↓
[前端下载] loadZip - 解码并触发浏览器下载
```

### 1.2 详细流程解析

#### 阶段1：前端用户操作 (`ExportButton.tsx:9-47`)

**核心代码：**
```typescript
const handleClick = async () => {
  setIsLoading(true);
  
  const { zip } = await get(`/websites/${websiteId}/export`, {
    ...date,
    ...filters,
    ...searchParams,
    format: 'json',
  });

  await loadZip(zip);
  setIsLoading(false);
};
```

**关键步骤：**
- 收集当前页面的日期参数 (`useDateParameters`)
- 收集过滤条件参数 (`useFilterParameters`)
- 收集URL搜索参数 (`useSearchParams`)
- 调用导出API，传入所有查询条件
- 接收Base64编码的ZIP文件

#### 阶段2：API路由与参数验证 (`route.ts:9-21`)

**核心代码：**
```typescript
export async function GET(
  request: Request,
  { params }: { params: Promise<{ websiteId: string }> },
) {
  const schema = withDateRange({
    ...pagingParams,
  });

  const { auth, query, error } = await parseRequest(request, schema);
  
  if (error) return error();
  // ...
}
```

**参数验证层级 (`schema.ts`)：**
- `withDateRange`: 日期范围验证（`startAt`+`endAt` 或 `startDate`+`endDate` 必须提供）
- `pagingParams`: 分页参数验证（`page`, `pageSize`）
- `timezoneParam`: 时区有效性验证
- `unitParam`: 时间单位验证（year/month/hour/day/minute）

#### 阶段3：权限认证与授权

**3.1 身份认证 (`request.ts:12-53`)**

`parseRequest` 函数执行流程：
1. 解析URL查询参数和请求体
2. 使用Zod Schema验证参数合法性
3. 调用 `checkAuth(request)` 进行身份认证
4. 认证失败返回 `unauthorized()` 401响应

**3.2 身份认证细节 (`auth.ts:17-60`)**

```typescript
export async function checkAuth(request: Request) {
  const token = getBearerToken(request);
  const payload = parseSecureToken(token, secret());
  const shareToken = await parseShareToken(request);
  
  let user = null;
  const { userId, authKey } = payload || {};

  if (userId) {
    user = await getUser(userId);
  } else if (redis.enabled && authKey) {
    const key = await redis.client.get(authKey);
    if (key?.userId) {
      user = await getUser(key.userId);
    }
  }

  // 无用户且无共享令牌，认证失败
  if (!user?.id && !shareToken) {
    return null;
  }

  // 仅有共享令牌时，需要上下文头
  if (!user?.id && shareToken) {
    const shareContext = request.headers.get(SHARE_CONTEXT_HEADER);
    if (!shareContext) {
      return null;
    }
  }

  // 设置管理员标记
  if (user) {
    user.isAdmin = user.role === ROLES.admin;
  }
  
  return { token, authKey, shareToken, user };
}
```

**3.3 资源权限校验 (`website.ts:7-40`)**

```typescript
export async function canViewWebsite({ user, shareToken }: Auth, websiteId: string) {
  // 校验1：管理员直接放行（优先级最高）
  if (user?.isAdmin) return true;
  
  // 校验2：共享Token校验
  if (
    shareToken?.websiteId === websiteId ||
    shareToken?.pixelId === websiteId ||
    shareToken?.linkId === websiteId ||
    shareToken?.websiteIds?.includes(websiteId) ||
    shareToken?.pixelIds?.includes(websiteId) ||
    shareToken?.linkIds?.includes(websiteId)
  ) {
    return true;
  }
  
  // 校验3：实体归属校验
  const entity = await getEntity(websiteId);
  
  if (!entity || !user) {
    return false;
  }

  if (entity.userId) {
    return user.id === entity.userId;  // 个人网站
  }
  if (entity.teamId) {
    const teamUser = await getTeamUser(entity.teamId, user.id);
    return !!teamUser;  // 团队成员
  }
  
  return false;
}
```

**权限校验失败处理：** 返回 `unauthorized()` 401响应

#### 阶段4：查询条件组装 (`request.ts:111-178`)

**核心函数 `getQueryFilters` 执行流程：**

```typescript
export async function getQueryFilters(params, websiteId): Promise<QueryFilters> {
  // 1. 解析日期范围
  const dateRange = getRequestDateRange(params);
  
  // 2. 解析过滤条件（仅保留白名单字段）
  const filters = getRequestFilters(params);
  
  // 3. 应用网站日期限制（云模式订阅检查）
  await setWebsiteDate(websiteId, dateRange);
  
  // 4. 处理细分/群组参数
  if (params.segment) { /* 合并细分过滤条件 */ }
  if (params.cohort)  { /* 合并群组过滤条件 */ }
  if (params.excludeBounce) { filters.excludeBounce = true; }
  
  return {
    ...dateRange,
    ...filters,
    match, page, pageSize, orderBy, sortDescending, search, compare
  };
}
```

**云模式数据限制 (`request.ts:92-109`)：**
```typescript
if (cloudMode && website && !website.teamId) {
  const account = await fetchAccount(website.userId);
  if (!account?.hasSubscription) {
    // 非订阅用户最多只能查询6个月内的数据
    data.startDate = maxDate(data.startDate, startOfMonth(subMonths(new Date(), 6)));
  }
}
```

#### 阶段5：并行数据查询 (`route.ts:31-39`)

导出功能一次性查询7类数据指标，全部并行执行：

```typescript
const [events, pages, referrers, browsers, os, devices, countries] = await Promise.all([
  getEventMetrics(websiteId, { type: 'event' }, filters),
  getPageviewMetrics(websiteId, { type: 'path' }, filters),
  getPageviewMetrics(websiteId, { type: 'referrer' }, filters),
  getSessionMetrics(websiteId, { type: 'browser' }, filters),
  getSessionMetrics(websiteId, { type: 'os' }, filters),
  getSessionMetrics(websiteId, { type: 'device' }, filters),
  getSessionMetrics(websiteId, { type: 'country' }, filters),
]);
```

**查询函数通用结构 (`getEventMetrics.ts:21-96`)：**
```typescript
export async function getEventMetrics(websiteId, parameters, filters) {
  return runQuery({
    [PRISMA]: () => relationalQuery(...args),      // PostgreSQL
    [CLICKHOUSE]: () => clickhouseQuery(...args),  // ClickHouse
  });
}
```

**数据库路由 (`db.ts:22-36`)：**
- 配置了 `CLICKHOUSE_URL` 时使用 ClickHouse
- 否则根据 `DATABASE_URL` 协议判断使用 PostgreSQL

#### 阶段6：文件生成与编码 (`route.ts:41-61`)

```typescript
const zip = new JSZip();

const parse = (data: any) => {
  return Papa.unparse(data, {
    header: true,
    skipEmptyLines: true,
  });
};

zip.file('events.csv', parse(events));
zip.file('pages.csv', parse(pages));
zip.file('referrers.csv', parse(referrers));
zip.file('browsers.csv', parse(browsers));
zip.file('os.csv', parse(os));
zip.file('devices.csv', parse(devices));
zip.file('countries.csv', parse(countries));

const content = await zip.generateAsync({ type: 'nodebuffer' });
const base64 = content.toString('base64');

return json({ zip: base64 });
```

**生成的文件包结构：**
```
download.zip
├── events.csv      # 自定义事件指标
├── pages.csv       # 页面访问路径指标
├── referrers.csv   # 来源域名指标
├── browsers.csv    # 浏览器分布
├── os.csv          # 操作系统分布
├── devices.csv     # 设备类型分布
└── countries.csv   # 国家/地区分布
```

#### 阶段7：前端下载处理 (`ExportButton.tsx:49-64`)

```typescript
async function loadZip(zip: string) {
  // Base64解码
  const binary = atob(zip);
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i++) {
    bytes[i] = binary.charCodeAt(i);
  }

  // 创建Blob并触发下载
  const blob = new Blob([bytes], { type: 'application/zip' });
  const url = URL.createObjectURL(blob);
  
  const a = document.createElement('a');
  a.href = url;
  a.download = 'download.zip';
  a.click();
  URL.revokeObjectURL(url);
}
```

---

## 二、导出文件列结构与字段差异

### 2.1 各查询函数的实际返回字段

#### 2.1.1 getEventMetrics - events.csv

**接口定义 (`getEventMetrics.ts:15-19`)：**
```typescript
export interface EventMetricData {
  x: string;
  t: string;
  y: number;
}
```

**实际 SQL 查询（PostgreSQL 版本 `getEventMetrics.ts:47-64`）：**
```sql
select ${column} x,
  count(*) as y
from website_event
-- ...
```

**ClickHouse 版本（`getEventMetrics.ts:81-96`）：**
```sql
select ${column} x,
  count(*) as y
from website_event
-- ...
```

**重要发现**：接口定义中的 `t` 字段**在实际SQL查询中并未被SELECT**，只查询了 `x` 和 `y` 两列。因此 `events.csv` 实际只有两列。

---

#### 2.1.2 getPageviewMetrics - pages.csv, referrers.csv

**接口定义 (`getPageviewMetrics.ts:15-18`)：**
```typescript
export interface PageviewMetricsData {
  x: string;
  y: number;
}
```

**实际 SQL 查询（PostgreSQL 版本 `getPageviewMetrics.ts:73-95`）：**
```sql
select ${column} x,
  count(distinct website_event.session_id) as y
from website_event
-- ...
```

**ClickHouse 版本（`getPageviewMetrics.ts:136-153`）：**
```sql
select ${column} x, 
  uniq(website_event.session_id) as y
from website_event
-- ...
```

**实际列**：`x`（维度值）、`y`（会话去重计数）

---

#### 2.1.3 getSessionMetrics - browsers.csv, os.csv, devices.csv, countries.csv

**接口定义**：无显式接口定义，返回类型为 `Promise<{ x: string; y: number }[]>`

**实际 SQL 查询（PostgreSQL 版本 `getSessionMetrics.ts:48-71`）：**
```sql
select 
  ${column} x,
  count(distinct website_event.session_id) y
  ${includeCountry ? ', country' : ''}
from website_event
-- ...
```

**特殊情况**：当 `type` 为 `'city'` 或 `'region'` 时，会额外返回 `country` 字段（`includeCountry = column === 'city' || column === 'region'`）。

**但导出接口中使用的 type 为**：
- `type: 'browser'` → browsers.csv
- `type: 'os'` → os.csv
- `type: 'device'` → devices.csv
- `type: 'country'` → countries.csv

这些 type 都**不会触发** `includeCountry` 条件，因此导出的4个文件都只有 `x` 和 `y` 两列。

---

### 2.2 7个导出文件的列结构对比

| 文件名 | 数据来源 | type参数 | 映射列 | 实际列 | 说明 |
|-------|---------|---------|--------|--------|------|
| **events.csv** | `getEventMetrics` | `'event'` | `event_name` | `x, y` | 接口定义有 `t` 但实际未查询 |
| **pages.csv** | `getPageviewMetrics` | `'path'` | `url_path` | `x, y` | y = 去重会话数 |
| **referrers.csv** | `getPageviewMetrics` | `'referrer'` | `referrer_domain` | `x, y` | y = 去重会话数 |
| **browsers.csv** | `getSessionMetrics` | `'browser'` | `browser` | `x, y` | y = 去重会话数 |
| **os.csv** | `getSessionMetrics` | `'os'` | `os` | `x, y` | y = 去重会话数 |
| **devices.csv** | `getSessionMetrics` | `'device'` | `device` | `x, y` | y = 去重会话数 |
| **countries.csv** | `getSessionMetrics` | `'country'` | `country` | `x, y` | y = 去重会话数 |

**关键结论**：
1. **所有7个CSV文件实际都只有 `x` 和 `y` 两列**
2. `events.csv` 的接口定义中的 `t` 字段是冗余的，实际数据中不存在
3. 各文件的 `y` 列统计口径不同：
   - events.csv: `count(*)` - 事件发生次数
   - pages/referrers: `count(distinct session_id)` - 独立会话数
   - browsers/os/devices/countries: `count(distinct session_id)` - 独立会话数

---

## 三、参数透传与分页行为

### 3.1 ExportButton 前端参数组装

`ExportButton.tsx:17-25` 发起请求时，将三类参数合并传入 `get` 方法：

```typescript
const { zip } = await get(`/websites/${websiteId}/export`, {
  ...date,         // useDateParameters()
  ...filters,      // useFilterParameters()
  ...searchParams, // useSearchParams() ← ⚠️ URLSearchParams 对象，展开后为空
  format: 'json',
});
```

#### 3.1.1 `...searchParams` 的实际展开行为

**重要修正：** `useSearchParams()` 返回的是 Next.js 的 `URLSearchParams` 对象，不是普通 JavaScript 对象。当对 `URLSearchParams` 使用展开运算符 `...` 时，**不会展开为键值对**。

**代码验证（Node.js）：**
```javascript
const sp = new URLSearchParams('foo=bar&baz=qux');
console.log({...sp});  // 输出: {}
// URLSearchParams 是一个可迭代对象，但不是可枚举对象
// 展开运算符只复制对象自身的可枚举属性
```

**实际合并效果：**
```javascript
const date = { startAt: 1703980800000, endAt: 1704067200000 };
const filters = { page: 1, pageSize: 20, browser: 'eq.Chrome' };
const searchParams = new URLSearchParams('other=value');

const merged = {
  ...date,
  ...filters,
  ...searchParams,  // ← 展开后是空对象 {}，不贡献任何参数
  format: 'json',
};

// 最终 merged 对象：
{
  startAt: 1703980800000,
  endAt: 1704067200000,
  page: 1,
  pageSize: 20,
  browser: 'eq.Chrome',
  format: 'json'
}
```

**结论：`...searchParams` 展开后不贡献任何参数，实际生效的参数全部来自 `...date` 和 `...filters`。**

#### 3.1.2 三个参数源的实际作用范围

| 参数源 | 返回类型 | 展开后是否贡献参数 | 实际作用 |
|-------|---------|------------------|---------|
| `...date` | 普通对象 | ✅ 是 | 提供日期时间参数 |
| `...filters` | 普通对象 | ✅ 是 | 提供过滤条件和分页参数 |
| `...searchParams` | `URLSearchParams` | ❌ **否（展开后为空对象）** | 不贡献任何参数 |

#### 3.1.3 各参数源的具体字段

**1. `date` = `useDateParameters()` (`useDateParameters.ts:4-18`)**

```typescript
return {
  startAt: +localToUtc(startDate),       // 时间戳（数字）
  endAt: +localToUtc(endDate),          // 时间戳（数字）
  startDate: localToUtc(startDate).toISOString(),  // ISO字符串
  endDate: localToUtc(endDate).toISOString(),      // ISO字符串
  unit,                                     // 时间单位（hour/day/week等）
  timezone: canonicalizeTimezone(timezone),      // 时区（如Asia/Shanghai）
};
```

**2. `filters` = `useFilterParameters()` (`useFilterParameters.ts:5-28`)**

```typescript
// useFilterParameters 内部通过 useNavigation().query 获取参数
// useNavigation().query = Object.fromEntries(useSearchParams()) ← 这才是真正的对象转换
const { query } = useNavigation();

return {
  ...filterParams,       // FILTER_COLUMNS 白名单字段（browser, os, device等）
  search: query.search,
  segment: query.segment,
  cohort: query.cohort,
  excludeBounce: query.excludeBounce,
  match: query.match,
  page: query.page,
  pageSize: query.pageSize,
};
```

**关键区别：** `useFilterParameters()` → `useNavigation().query` → `Object.fromEntries(searchParams)`，这才是正确的 `URLSearchParams` → 普通对象转换方式。

**3. `searchParams` = `useSearchParams()` (Next.js)**

返回 `URLSearchParams` 对象，展开后为空对象，不贡献任何参数。

#### 3.1.4 两种 URL 参数读取方式的对比

| 方式 | 代码 | 结果类型 | 展开效果 |
|-----|------|---------|---------|
| ❌ 错误方式 | `useSearchParams()` 直接展开 | `URLSearchParams` | 空对象 `{}` |
| ✅ 正确方式 | `Object.fromEntries(useSearchParams())` | 普通对象 | 键值对 `{key: value}` |

**ExportButton 中的冗余：** `...searchParams` 的展开是无效的，所有URL参数已经通过 `useFilterParameters()` → `useNavigation().query` 路径正确获取并传递了。

---

### 3.2 前端参数到URL查询字符串的序列化

`useApi.ts:46-47` 中 `get` 方法调用 `httpGet`：

```typescript
get: useCallback(
  async (url: string, params: object = {}, headers: object = {}) => {
    return httpGet(getUrl(url), params, getHeaders(headers)).then(handleResponse);
  },
  [httpGet],
),
```

`fetch.ts:44-46` 中 `httpGet` 调用 `buildPath`：

```typescript
export async function httpGet(path: string, params: object = {}, headers: object = {}) {
  return request('GET', buildPath(path, params), undefined, headers);
}
```

`url.ts:13-16` 中 `buildPath` 将参数序列化为URL查询字符串：

```typescript
export function buildPath(path: string, params: object = {}): string {
  const queryString = getQueryString(params);
  return queryString ? `${path}?${queryString}` : path;
}

export function getQueryString(params: object = {}): string {
  const searchParams = new URLSearchParams();
  Object.entries(params).forEach(([key, value]) => {
    if (value !== undefined) {
      searchParams.append(key, value);
    }
  });
  return searchParams.toString();
}
```

**结论：只有来自 `...date` 和 `...filters` 的参数会被序列化到URL查询字符串中发送到后端。**

**实际请求示例：**
```
GET /api/websites/{websiteId}/export?startAt=1703980800000&endAt=1704067200000&unit=day&timezone=Asia%2FShanghai&browser=eq.Chrome&page=1&pageSize=20&format=json
```

---

### 3.3 过滤条件透传的完整证据链

#### 3.3.1 证据链总览

```
浏览器URL: ?page=1&pageSize=20&browser=eq.Chrome&startAt=xxx&endAt=xxx
    ↓
[Hook 1] useSearchParams() → 返回 URLSearchParams 对象
    ↓
[Hook 2] useNavigation() → Object.fromEntries(searchParams) → 普通对象
    ↓  query = { page: "1", pageSize: "20", browser: "eq.Chrome", startAt: "xxx", ... }
[Hook 3] useFilterParameters(query)
    ↓  白名单过滤 + 额外字段提取
    ↓  filters = { browser: "eq.Chrome", page: "1", pageSize: "20", ... }
[ExportButton] 参数合并
    ↓  { ...date, ...filters, ...searchParams（空）, format: "json" }
    ↓  merged = { startAt: xxx, endAt: xxx, browser: "eq.Chrome", page: 1, pageSize: 20, ... }
[HTTP] buildPath(merged)
    ↓  URL: ?startAt=xxx&endAt=xxx&browser=eq.Chrome&page=1&pageSize=20&format=json
    ↓
[后端 parseRequest] Zod Schema 验证
    ↓  schema = withDateRange({ ...pagingParams })
    ↓  只声明了: startAt, endAt, startDate, endDate, timezone, unit, compare, page, pageSize
    ↓  browser 未声明 → 被 Zod 过滤掉
    ↓  query = { startAt: xxx, endAt: xxx, page: 1, pageSize: 20 }
[后端 getQueryFilters(query, websiteId)]
    ↓  getRequestFilters(query) → 遍历 query 的 key，检查是否在 FILTER_COLUMNS 中
    ↓  此时 query 中已经没有 browser 了，因为被 Zod 过滤了
    ↓  filters = {}
    ↓  返回 QueryFilters { startDate: Date, endDate: Date, page: 1, pageSize: 20, ... }
[后端 route.ts] 调用 getEventMetrics(websiteId, { type: 'event' }, filters)
    ↓  parameters = { type: 'event' }
    ↓  filters = { startDate: Date, endDate: Date, page: 1, pageSize: 20 }
    ↓
[getEventMetrics 内部] const { type, limit = 500, offset = 0 } = parameters
    ↓  limit 和 offset 从 parameters 读取，不从 filters 读取
    ↓  page 和 pageSize 存在于 filters 中但从未被使用
    ↓  SQL: LIMIT 500 OFFSET 0
```

#### 3.3.2 无后缀键与后缀键的差异化处理路径

这是导出链路中最关键也是最容易被忽略的设计细节：**无后缀键（如 `browser`）和带后缀键（如 `browser1`）在 parseRequest 中有完全不同的命运**。

**核心机制代码** (`request.ts:23-42`)：
```typescript
if (schema) {
  const isGet = request.method === 'GET';
  const rawQuery = query;
  const result = schema.safeParse(isGet ? query : body);

  if (!result.success) {
    error = () => badRequest(z.treeifyError(result.error));
  } else if (isGet) {
    query = result.data;  // ← 步骤1：只保留 schema 中声明的字段

    // 步骤2：回补带数字后缀的字段（如 browser1, os2）
    for (const key of Object.keys(rawQuery)) {
      if (/\d+$/.test(key) && !(key in query)) {
        query[key] = rawQuery[key];
      }
    }
  }
}
```

**两种键的处理路径对比**：

| 处理阶段 | 无后缀键（如 `browser`） | 带后缀键（如 `browser1`） |
|---------|-----------------------|-------------------------|
| **步骤1：Schema过滤** | 不在Schema中声明 → 被 Zod 删除 | 不在Schema中声明 → 被 Zod 删除 |
| **步骤2：回补检查** | 无数字后缀 → `/\d+$/` 不匹配 → 不回补 | 有数字后缀 → `/\d+$/` 匹配 → 回补到 query |
| **最终结果** | **❌ 完全丢失** | **✅ 成功保留** |

**测试验证结果**：
```
原始URL参数: browser=eq.Chrome, browser1=eq.Firefox, browser2=eq.Safari, os=eq.Windows, os1=eq.macOS, device=eq.Desktop
Schema过滤后保留: [ startAt, endAt, page, pageSize ]
回补的键: browser1, browser2, os1
最终query中的键: [ startAt, endAt, page, pageSize, browser1, browser2, os1 ]
```

**结论**：
- 无后缀键（单个过滤条件）：会被完全丢弃
- 带后缀键（多个过滤条件）：会被回补保留
- 导出接口**不支持单个过滤条件**，只支持**多个过滤条件**

---

#### 3.3.3 进入 getRequestFilters 和 SQL 查询的差异化证据链

**完整的8节点证据链（以 `browser` 和 `browser1` 为例）**：

| 节点 | 代码位置 | `browser`（无后缀） | `browser1`（带后缀） |
|-----|---------|-------------------|---------------------|
| **1. 浏览器URL** | 用户输入 | `?browser=eq.Chrome` | `?browser1=eq.Firefox` |
| **2. useNavigation.query** | `useNavigation.ts:14` | ✅ 正确提取 | ✅ 正确提取 |
| **3. useFilterParameters** | `useFilterParameters.ts:8-27` | ✅ baseName=browser 匹配白名单 | ✅ baseName=browser 匹配白名单 |
| **4. ExportButton合并** | `ExportButton.tsx:20-25` | ✅ 来自 `...filters` | ✅ 来自 `...filters` |
| **5. Zod Schema验证** | `request.ts:26` | ❌ 不在Schema中 → 删除 | ❌ 不在Schema中 → 删除 |
| **6. 数字后缀回补** | `request.ts:33-38` | ❌ 无后缀 → 不回补 | ✅ 有后缀 → 回补 |
| **7. getRequestFilters** | `request.ts:79-90` | ❌ 不在query中 → 丢失 | ✅ baseName匹配白名单 → 进入filters |
| **8. SQL查询构建** | `prisma.ts:108-150` | ❌ 不出现 | ✅ 成功进入 WHERE 子句 |

**节点6的回补逻辑关键代码** (`request.ts:33-38`)：
```typescript
// Re-add suffixed filter params (e.g., browser1, os2) stripped by Zod schema
for (const key of Object.keys(rawQuery)) {
  if (/\d+$/.test(key) && !(key in query)) {  // 两个条件：以数字结尾 && 不在已解析的query中
    query[key] = rawQuery[key];
  }
}
```

**节点7的白名单过滤** (`request.ts:79-90`)：
```typescript
export function getRequestFilters(query: Record<string, any>) {
  const result: Record<string, any> = {};
  for (const key of Object.keys(query)) {
    const baseName = key.replace(/\d+$/, '');  // 去掉数字后缀：browser1 → browser
    if (baseName in FILTER_COLUMNS) {          // 检查基础名称是否在白名单中
      result[key] = query[key];
    }
  }
  return result;
}
```

**最终SQL构建** (`prisma.ts:114-139`)：
```typescript
filtersObjectToArray(filters, options).forEach(
  ({ name, column, operator, prefix = '', paramName }) => {
    // paramName = browser1（保留原始键名，用于参数绑定）
    // name = browser（去掉后缀后的基础名称）
    // column = browser（数据库列名）
    const clause = mapFilter(`${prefix}${column}`, operator, name, '', paramName);
    // ...
  }
);
```

**关键发现**：
- `paramName` 保留原始的带后缀键名（如 `browser1`），用于SQL参数绑定时避免命名冲突
- `name` 是去掉后缀后的基础名称，用于判断表归属（`SESSION_COLUMNS` 或 `website_event`）
- `column` 是最终映射的数据库列名

---

#### 3.3.4 关键节点的代码证据

**节点1：useNavigation 正确转换 URLSearchParams** (`useNavigation.ts:14`)
```typescript
const [queryParams, setQueryParams] = useState(Object.fromEntries(searchParams));
// ✅ 正确转换：URLSearchParams → 普通对象
```

**节点2：useFilterParameters 提取过滤参数** (`useFilterParameters.ts:8-27`)
```typescript
const filterParams: Record<string, any> = {};
for (const key of Object.keys(query)) {
  const baseName = key.replace(/\d+$/, '');
  if (FILTER_COLUMNS[baseName]) {  // 白名单检查
    filterParams[key] = query[key];
  }
}
return {
  ...filterParams,  // 包含 browser, os, device 等过滤条件
  search: query.search,
  segment: query.segment,
  cohort: query.cohort,
  excludeBounce: query.excludeBounce,
  match: query.match,
  page: query.page,
  pageSize: query.pageSize,
};
```

**节点3：ExportButton 中的无效展开** (`ExportButton.tsx:20-25`)
```typescript
{
  ...date,
  ...filters,      // ✅ 这才是所有参数的来源（包括过滤条件和page/pageSize）
  ...searchParams, // ❌ 无效，展开后为空对象
  format: 'json',
}
```

**节点4：parseRequest 的 Zod 过滤** (`request.ts:23-42`)
```typescript
const result = schema.safeParse(isGet ? query : body);
if (!result.success) {
  error = () => badRequest(...);
} else if (isGet) {
  query = result.data;  // ← 只保留 schema 中声明的字段
  // 只回补带数字后缀的字段（如 browser1, os2）
  for (const key of Object.keys(rawQuery)) {
    if (/\d+$/.test(key) && !(key in query)) {
      query[key] = rawQuery[key];
    }
  }
}
```

**节点5：导出路由 Schema 声明不完整** (`route.ts:13-15`)
```typescript
const schema = withDateRange({
  ...pagingParams,  // 只声明了 page 和 pageSize
  // ❌ 缺少 ...filterParams，所以 browser 等过滤参数会被 Zod 丢弃
});
```

**节点6：getRequestFilters 无法恢复被 Zod 过滤的参数** (`request.ts:79-90`)
```typescript
export function getRequestFilters(query: Record<string, any>) {
  const result: Record<string, any> = {};
  for (const key of Object.keys(query)) {  // 遍历的是已经被 Zod 过滤后的 query
    const baseName = key.replace(/\d+$/, '');
    if (baseName in FILTER_COLUMNS) {
      result[key] = query[key];
    }
  }
  return result;  // ❌ query 中已经没有 browser 了，所以返回空对象
}
```

**节点7：getQueryFilters 将 page/pageSize 写入 filters** (`request.ts:171-172`)
```typescript
return {
  ...dateRange,
  ...filters,  // 此时 filters 是空对象，因为 browser 被 Zod 过滤了
  match,
  page: params?.page,                                                    // ✅ 写入
  pageSize: params?.pageSize ? params?.pageSize || DEFAULT_PAGE_SIZE : undefined,  // ✅ 写入
  orderBy: params?.orderBy,
  sortDescending: params?.sortDescending,
  search: params?.search,
  compare: params?.compare,
};
```

**节点8：get*Metrics 不从 filters 读取 page/pageSize** (`getEventMetrics.ts:35`)
```typescript
const { type, limit = 500, offset = 0 } = parameters;  // ← 从 parameters 解构
// ❌ page 和 pageSize 在 filters 中，但函数不读取
// ❌ parameters 中只有 type，所以 limit 和 offset 使用默认值 500 和 0
```

---

### 3.4 对比：metrics路由的Schema声明

`metrics/route.ts:18-24` 的Schema声明了过滤参数：

```typescript
const schema = withDateRange({
  type: z.string(),
  limit: z.coerce.number().optional(),
  offset: z.coerce.number().optional(),
  ...searchParams,
  ...filterParams,   // ← ✅ 包含 browser, os, device 等过滤参数
});
```

**对比结论：**

| 字段 | export路由 | metrics路由 | 影响 |
|------|----------|-----------|------|
| 日期参数 | ✅ 声明 | ✅ 声明 | 两个路由都支持 |
| `page`, `pageSize` | ✅ 声明 | ❌ 未声明 | export路由接受但不使用（见3.5节） |
| `type`, `limit`, `offset` | ❌ 未声明 | ✅ 声明 | metrics路由支持自定义limit，export硬编码500 |
| 过滤参数 (`browser`等) | ❌ 未声明 | ✅ 声明 | **export路由的前端过滤参数被Zod丢弃** |
| `search` | ❌ 未声明 | ✅ 声明 | export不支持搜索 |

---

### 3.5 page/pageSize 的完整作用路径

**路径追踪：**

```
浏览器URL: ?page=1&pageSize=20
    ↓
useNavigation() → Object.fromEntries(searchParams)
    ↓  query = { page: "1", pageSize: "20", ... }
useFilterParameters()
    ↓  filters = { page: "1", pageSize: "20", ... }
ExportButton 参数合并
    ↓  merged = { page: "1", pageSize: "20", ... }
buildPath() 序列化
    ↓  URL: ?page=1&pageSize=20
    ↓
parseRequest(request, schema)
    ↓  Zod Schema 验证通过（pagingParams已声明）
    ↓  类型转换："1" → 1, "20" → 20
    ↓  query = { startAt: ..., endAt: ..., page: 1, pageSize: 20 }
getQueryFilters(query, websiteId)
    ↓  page: params?.page                       → page: 1
    ↓  pageSize: params?.pageSize || DEFAULT_PAGE_SIZE → pageSize: 20
    ↓  返回 QueryFilters { ..., page: 1, pageSize: 20 }
route.ts 调用 getEventMetrics(websiteId, { type: 'event' }, filters)
    ↓  parameters = { type: 'event' }  ← 只有type，没有limit/offset
    ↓  filters = { ..., page: 1, pageSize: 20 }
getEventMetrics 内部
    ↓  const { type, limit = 500, offset = 0 } = parameters
    ↓  ↑ limit/offset 从 parameters 解构，不从 filters 读取
    ↓  page/pageSize 存在于 filters 中但从未被使用
    ↓  SQL: LIMIT 500 OFFSET 0
```

**关键代码证据：**

1. **getQueryFilters 将 page/pageSize 写入 filters** (`request.ts:171-172`)：
```typescript
return {
  ...dateRange,
  ...filters,
  match,
  page: params?.page,                                                    // ← 写入
  pageSize: params?.pageSize ? params?.pageSize || DEFAULT_PAGE_SIZE : undefined,  // ← 写入
  orderBy: params?.orderBy,
  sortDescending: params?.sortDescending,
  search: params?.search,
  compare: params?.compare,
};
```

2. **get*Metrics 从 parameters 读取 limit/offset，不从 filters 读取 page/pageSize** (`getEventMetrics.ts:35`)：
```typescript
const { type, limit = 500, offset = 0 } = parameters;  // ← 只从parameters解构
```

3. **导出路由调用时不传 limit/offset** (`route.ts:32`)：
```typescript
getEventMetrics(websiteId, { type: 'event' }, filters)
//                      parameters = { type: 'event' }  ← 无limit/offset
//                      filters.page = 1, filters.pageSize = 20  ← 存在但无用
```

4. **对比：metrics路由调用时显式传 limit/offset** (`metrics/route.ts:46`)：
```typescript
const { type, limit, offset, search } = query;
// ...
getSessionMetrics(websiteId, { type, limit, offset }, filters);
//                      parameters = { type, limit, offset }  ← 从URL参数读取
```

---

### 3.6 page/pageSize 的实际影响总结

| 阶段 | page/pageSize 是否存在 | 是否生效 | 说明 |
|------|----------------------|---------|------|
| 浏览器URL | ✅ 存在 | - | 用户可能在页面上设置了分页 |
| `useNavigation().query` | ✅ 转换为字符串 | - | `Object.fromEntries()` 正确转换 |
| `useFilterParameters()` | ✅ 提取 | - | 显式提取 `page` 和 `pageSize` |
| `ExportButton` 合并 | ✅ 包含 | - | 来自 `...filters` |
| `buildPath` 序列化 | ✅ 到URL | - | 传入后端 |
| 后端 Schema 验证 | ✅ 通过验证 | - | `pagingParams` 已在Schema中声明 |
| 后端 `parseRequest` | ✅ 保留在 `query` | - | 类型转换为number |
| 后端 `getQueryFilters` | ✅ 写入 `QueryFilters` | - | 作为filters对象的属性传递 |
| 后端 `get*Metrics` 调用 | ✅ 存在于filters | ❌ **不生效** | 函数从 `parameters` 读取 `limit/offset`，不读取 `filters.page/pageSize` |
| SQL查询 | ❌ 不存在 | ❌ **不生效** | 使用默认 `LIMIT 500 OFFSET 0` |

**最终结论：`page` 和 `pageSize` 在导出链路中经历了完整的前端→后端透传，但最终不产生任何实际效果。导出查询固定使用 `LIMIT 500 OFFSET 0`。**

---

### 3.7 前端过滤参数的透传问题

**问题根源：** 前端 `useFilterParameters` 会提取URL中的过滤参数（如 `browser=eq.Chrome`），但由于导出路由的Schema未声明 `filterParams`，这些参数在 `parseRequest` 的Zod验证阶段被丢弃。

**完整证据链（以 `browser=eq.Chrome` 为例）：**

| 节点 | 值 | 说明 |
|-----|----|------|
| 浏览器URL | `?browser=eq.Chrome` | ✅ 用户在页面上筛选了Chrome浏览器 |
| `useNavigation().query` | `{ browser: "eq.Chrome" }` | ✅ 正确转换为对象 |
| `useFilterParameters()` | `{ browser: "eq.Chrome" }` | ✅ 白名单匹配（FILTER_COLUMNS["browser"] = "browser"） |
| `ExportButton` 合并 | `{ browser: "eq.Chrome" }` | ✅ 来自 `...filters` |
| `buildPath` 序列化 | `?browser=eq.Chrome` | ✅ 传入后端 |
| URL `searchParams` 解析 | `{ browser: "eq.Chrome" }` | ✅ 后端解析URL |
| **Zod Schema 验证** | **被过滤** | ❌ Schema 未声明 `browser` 字段，被 Zod 的 `safeParse` 丢弃 |
| `parseRequest` 返回的 `query` | `{}`（无browser） | ❌ 已经没有了 |
| `getRequestFilters(query)` | `{}` | ❌ 遍历 query 时找不到 browser |
| `QueryFilters` | `{}`（无browser过滤条件） | ❌ SQL 不包含浏览器过滤条件 |
| SQL查询 | `WHERE ...`（无 browser = 'Chrome'） | ❌ 返回所有浏览器的数据 |

**影响：**
- 用户在页面上设置的过滤条件（如筛选特定浏览器、操作系统、设备等）**不会影响导出结果**
- 导出始终返回网站在指定时间范围内的全部7类指标数据
- 这与 `metrics` 路由的行为形成鲜明对比：`metrics` 路由声明了 `filterParams`，能正确响应过滤条件

**各类参数透传情况汇总：**

| 参数类型 | 前端传入 | Schema验证 | 写入filters | 查询生效 |
|---------|---------|-----------|------------|---------|
| 日期参数 (`startAt`, `endAt`) | ✅ | ✅ 通过 | ✅ | ✅ 生效 |
| `page`, `pageSize` | ✅ | ✅ 通过 | ✅ | ❌ 不生效（查询不读取） |
| 过滤参数 (`browser`, `os`等) | ✅ | ❌ **被Zod丢弃** | ❌ | ❌ 不生效 |
| `search` | ✅ | ❌ 被Zod丢弃 | ❌ | ❌ 不生效 |
| `segment`, `cohort` | ✅ | ❌ 被Zod丢弃 | ❌ | ❌ 不生效 |
| `match` | ✅ | ❌ 被Zod丢弃 | ❌ | ❌ 不生效 |
| `excludeBounce` | ✅ | ❌ 被Zod丢弃 | ❌ | ❌ 不生效 |

**注意：** `segment` 和 `cohort` 虽然在 `useFilterParameters` 中被提取，但导出路由的Schema中未声明这两个参数（Schema只在 `filterParams` 中声明了 `segment` 和 `cohort`，而导出路由未引入 `filterParams`）。因此即使用户在页面上选择了细分或群组，导出也不会应用这些条件。

**设计意图推测：** 导出功能可能设计为始终导出完整数据集，不受当前页面过滤条件影响。但这种实现方式存在以下问题：
1. 用户体验不一致：页面显示过滤后的数据，但导出的是全部数据
2. 代码冗余：`useFilterParameters` 中提取的过滤条件完全无效
3. `...searchParams` 展开无效，属于代码冗余
4. `pagingParams` 在 Schema 中声明但最终不生效

---

## 四、同步/异步导出机制

### 4.1 结论：**同步导出**

当前实现采用**完全同步**的处理方式：

**证据：**
1. `route.ts` 的 `GET` 方法是标准的 HTTP 请求-响应模式
2. 所有数据查询在同一请求线程内通过 `Promise.all` 并行执行
3. 文件生成（CSV解析、ZIP打包）在同一请求内完成
4. 响应直接返回完整的Base64编码文件内容
5. 前端使用 `await` 同步等待整个过程完成

**执行时序图：**
```
前端线程                    后端线程
   |                           |
   | ---- GET /export -------> |
   |                           | 1. 参数验证
   |                           | 2. 权限校验
   |                           | 3. 查询组装
   |                           | 4. 并行查询7类数据
   |                           |    (Promise.all)
   |                           | 5. 生成7个CSV文件
   |                           | 6. ZIP打包
   |                           | 7. Base64编码
   | <---- {zip: base64} ----- |
   |                           |
   | 8. 解码下载               |
   |                           |
```

### 4.2 同步设计的优缺点

**优点：**
- 实现简单，无需任务队列、状态管理、回调通知
- 用户体验直接，点击后等待即可下载
- 无中间状态，便于调试和错误处理

**缺点：**
- 大数据量下请求超时风险高
- 长时间占用HTTP连接
- 不支持导出进度反馈
- 无法取消正在进行的导出任务

---

## 五、字段显示与角色权限控制

### 5.1 权限控制模型

**系统角色定义 (`constants.ts:164-172`)：**
```typescript
export const ROLES = {
  admin: 'admin',
  user: 'user',
  viewOnly: 'view-only',
  teamOwner: 'team-owner',
  teamManager: 'team-manager',
  teamMember: 'team-member',
  teamViewOnly: 'team-view-only',
} as const;
```

**角色权限映射 (`constants.ts:186-210`)：**
```typescript
export const ROLE_PERMISSIONS = {
  [ROLES.admin]: [PERMISSIONS.all],
  [ROLES.user]: [
    PERMISSIONS.websiteCreate,
    PERMISSIONS.websiteUpdate,
    PERMISSIONS.websiteDelete,
    PERMISSIONS.teamCreate,
  ],
  [ROLES.viewOnly]: [],  // 无任何操作权限
  // ... 团队角色权限
}
```

### 5.2 字段裁剪实现机制

**字段裁剪发生在**「SQL查询层 - SELECT子句级别」，通过以下机制实现：

#### 5.2.1 查询类型 -> 字段映射

在 `get*Metrics` 函数中，通过 `type` 参数动态选择查询字段：

```typescript
// getEventMetrics.ts:36,72
const column = FILTER_COLUMNS[type] || type;

// getPageviewMetrics.ts:35,104
let column = FILTER_COLUMNS[type] || type;

// getSessionMetrics.ts:30,80
let column = FILTER_COLUMNS[type] || type;
```

**FILTER_COLUMNS 映射表 (`constants.ts:72-97`)：**
```typescript
export const FILTER_COLUMNS = {
  path: 'url_path',
  entry: 'url_path',
  exit: 'url_path',
  referrer: 'referrer_domain',
  domain: 'referrer_domain',
  hostname: 'hostname',
  distinctId: 'distinct_id',
  title: 'page_title',
  query: 'url_query',
  os: 'os',
  browser: 'browser',
  device: 'device',
  country: 'country',
  region: 'region',
  city: 'city',
  language: 'language',
  event: 'event_name',
  tag: 'tag',
  eventType: 'event_type',
  utmSource: 'utm_source',
  utmMedium: 'utm_medium',
  utmCampaign: 'utm_campaign',
  utmContent: 'utm_content',
  utmTerm: 'utm_term',
};
```

#### 5.2.2 导出接口固定字段集

导出接口硬编码了7类固定查询，不接受外部动态字段参数：

```typescript
// route.ts:31-39
const [events, pages, referrers, browsers, os, devices, countries] = await Promise.all([
  getEventMetrics(websiteId, { type: 'event' }, filters),      // x: event_name
  getPageviewMetrics(websiteId, { type: 'path' }, filters),    // x: url_path
  getPageviewMetrics(websiteId, { type: 'referrer' }, filters),// x: referrer_domain
  getSessionMetrics(websiteId, { type: 'browser' }, filters),  // x: browser
  getSessionMetrics(websiteId, { type: 'os' }, filters),       // x: os
  getSessionMetrics(websiteId, { type: 'device' }, filters),   // x: device
  getSessionMetrics(websiteId, { type: 'country' }, filters),  // x: country
]);
```

**导出字段固定为：**
- `x`: 维度值（事件名/路径/域名/浏览器/OS/设备/国家）
- `y`: 统计值（计数或去重会话数）

### 5.3 角色对字段可见性的实际影响

**权限层级与控制粒度：**

| 权限层级 | 控制粒度 | 实现位置 | 对字段可见性的影响 |
|---------|---------|---------|-------------------|
| **资源级** | 网站/报表整体 | `canViewWebsite` | 无权限则完全无法访问，无导出机会 |
| **数据范围级** | 时间范围 | `setWebsiteDate` | 非订阅用户只能查询最近6个月数据（影响数据量，不影响字段） |
| **字段级** | **固定字段集，无角色差异** | SQL查询层 | 所有有权限用户看到相同字段（x, y两列） |

**关键结论**：

1. **管理员放行、共享令牌校验、实体归属校验都是「资源级权限」，不是「字段级权限」**
2. 这三个校验是**OR关系**，满足任一即可通过权限检查
3. **一旦通过权限校验，所有用户看到的字段完全相同**，都是固定的 `x` 和 `y` 两列
4. 字段裁剪只发生在 SQL 查询层，通过硬编码的 `type` 参数选择查询列，不随用户角色变化
5. 不同角色的差异在于「能不能导出」，而不是「能导出哪些字段」

**设计决策说明：**

当前导出功能**未实现基于角色的动态字段裁剪**，原因在于：
1. 导出的7类数据都是聚合统计指标（维度+计数），不涉及敏感明细
2. 通过 `canViewWebsite` 已确保只有授权用户才能导出
3. 导出接口固定字段集，不接受外部动态字段参数，降低注入风险
4. 如需字段级权限，应在查询函数中增加 `allowedFields` 参数过滤

---

## 六、管理员放行、共享令牌校验、实体归属校验的完整拦截流程

### 6.1 完整拦截流程图

```
HTTP请求到达
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 层级1：身份认证 (checkAuth)                         │
│ 1. 解析 Authorization Bearer Token                 │
│ 2. 解析 x-umami-share-token 共享令牌               │
│ 3. 解析用户信息（userId → getUser）                 │
│ 4. 设置 isAdmin 标记（user.role === 'admin'）      │
│ 5. 校验：无用户且无共享令牌 → 返回 null             │
│ 6. 校验：仅有共享令牌但无上下文头 → 返回 null       │
└─────────────────────────┬───────────────────────────┘
                          │
                          ▼  auth = { user, shareToken, ... }
┌─────────────────────────────────────────────────────┐
│ 层级2：资源授权 (canViewWebsite)                    │
│                                                          │
│ ┌─────────────────────────────────────────────────┐  │
│ │ 校验1：管理员放行（优先级最高）                  │  │
│ │ if (user?.isAdmin) return true;                 │  │
│ └───────────────────┬─────────────────────────────┘  │
│                     │ 是                             │
│                     ▼                                │
│                ✅ 通过，返回 true                     │
│                     │ 否                             │
│                     ▼                                │
│ ┌─────────────────────────────────────────────────┐  │
│ │ 校验2：共享令牌校验                              │  │
│ │ if (shareToken?.websiteId === websiteId ||      │  │
│ │     shareToken?.websiteIds?.includes(websiteId) │  │
│ │     // ... 其他ID匹配 )                          │  │
│ └───────────────────┬─────────────────────────────┘  │
│                     │ 是                             │
│                     ▼                                │
│                ✅ 通过，返回 true                     │
│                     │ 否                             │
│                     ▼                                │
│ ┌─────────────────────────────────────────────────┐  │
│ │ 校验3：实体归属校验                              │  │
│ │ 3.1 getEntity(websiteId) 并行查询4类实体        │  │
│ │     (website, link, pixel, board)               │  │
│ │ 3.2 无实体或无用户 → return false                │  │
│ │ 3.3 个人网站：user.id === entity.userId          │  │
│ │ 3.4 团队网站：getTeamUser(teamId, userId) 存在   │  │
│ └───────────────────┬─────────────────────────────┘  │
│                     │                                │
│                     ▼                                │
│           ✅ 通过 / ❌ 拒绝                           │
└─────────────────────────┬───────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          │ 拒绝                          │ 通过
          ▼                               ▼
    返回 401 Unauthorized           继续执行查询
```

### 6.2 各校验环节的代码实现

#### 6.2.1 管理员放行 (`website.ts:8-10`)

```typescript
// 优先级最高，第一个检查
if (user?.isAdmin) {
  return true;
}
```

**说明**：
- `isAdmin` 在 `checkAuth` 中设置：`user.isAdmin = user.role === ROLES.admin`
- 管理员无需后续任何校验，直接访问所有网站数据
- 对字段可见性的影响：可导出所有7个CSV文件的完整数据

#### 6.2.2 共享令牌校验 (`website.ts:12-21`)

```typescript
// 第二个检查，支持多种共享ID类型
if (
  shareToken?.websiteId === websiteId ||        // 单个网站共享
  shareToken?.pixelId === websiteId ||          // 单个Pixel共享
  shareToken?.linkId === websiteId ||           // 单个Link共享
  shareToken?.websiteIds?.includes(websiteId) || // 多个网站共享
  shareToken?.pixelIds?.includes(websiteId) ||   // 多个Pixel共享
  shareToken?.linkIds?.includes(websiteId)       // 多个Link共享
) {
  return true;
}
```

**说明**：
- 共享令牌通过 `x-umami-share-token` 请求头传递
- 在 `checkAuth` 中解析：`parseShareToken(request)`
- 共享令牌用户无需登录，但必须携带 `x-umami-share-context` 上下文头
- 对字段可见性的影响：可导出数据，但字段集与管理员相同（固定 x, y 两列）

#### 6.2.3 实体归属校验 (`website.ts:23-39`)

```typescript
// 第三个检查，查询实体归属
const entity = await getEntity(websiteId);  // 并行查询 website/link/pixel/board

if (!entity || !user) {
  return false;
}

// 个人网站
if (entity.userId) {
  return user.id === entity.userId;
}

// 团队网站
if (entity.teamId) {
  const teamUser = await getTeamUser(entity.teamId, user.id);
  return !!teamUser;
}

return false;
```

**实体查询 (`entity.ts:4-14`)：**
```typescript
export async function getEntity(entityId: string) {
  // 并行查询4类实体，返回第一个匹配的
  const [website, link, pixel, board] = await Promise.all([
    getWebsite(entityId),
    getLink(entityId),
    getPixel(entityId),
    getBoard(entityId),
  ]);

  return website || link || pixel || board;
}
```

**说明**：
- 个人网站：用户ID必须与实体创建者ID完全匹配
- 团队网站：用户必须是该团队的成员（通过 `getTeamUser` 查询）
- 对字段可见性的影响：可导出数据，但字段集与管理员相同（固定 x, y 两列）

### 6.3 三种权限对字段可见性的影响对比

| 权限类型 | 能否导出 | 可见字段 | 数据范围 | 说明 |
|---------|---------|---------|---------|------|
| **管理员** | ✅ 是 | `x, y`（所有7个文件） | 无限制 | `isAdmin = true`，直接放行 |
| **共享令牌** | ✅ 是 | `x, y`（所有7个文件） | 令牌授权的网站 | 无需登录，只需令牌匹配 |
| **个人所有者** | ✅ 是 | `x, y`（所有7个文件） | 自己创建的网站 | `user.id === entity.userId` |
| **团队成员** | ✅ 是 | `x, y`（所有7个文件） | 所属团队的网站 | `getTeamUser(teamId, userId)` 存在 |
| **未授权用户** | ❌ 否 | 无 | 无 | 返回 401 Unauthorized |

**关键结论**：
1. 三种权限校验是**OR关系**，满足任一即可通过
2. **所有通过校验的用户看到的字段完全相同**，都是 `x, y` 两列
3. 权限差异只影响「能不能导出」，不影响「能导出哪些字段」
4. 字段集由导出接口硬编码的7个查询决定，不随用户角色动态变化

---

## 七、大量数据处理：分批和限流措施

### 7.1 结论：**有限的分批措施，无限流机制**

### 7.2 已实现的分批措施

#### 7.2.1 查询结果限制（LIMIT子句）

所有 `get*Metrics` 查询都内置了 `LIMIT 500` 的默认限制：

```typescript
// getEventMetrics.ts:35,72
const { type, limit = 500, offset = 0 } = parameters;

// SQL查询中的LIMIT
order by y desc
limit ${limit}
offset ${offset}
```

**3个查询函数统一使用默认limit=500：**
- `getEventMetrics.ts:35` - 事件指标
- `getPageviewMetrics.ts:34,103` - 页面浏览指标
- `getSessionMetrics.ts:29,79` - 会话指标

#### 7.2.2 统计聚合优化

ClickHouse场景下，无事件维度过滤时自动使用预聚合表：

```typescript
// getPageviewMetrics.ts:114-193
if (EVENT_COLUMNS.some(item => Object.keys(filters).includes(item))) {
  // 有过滤条件：查询原始明细表 website_event
} else {
  // 无过滤条件：查询预聚合表 website_event_stats_hourly
  // 使用 arrayJoin + uniq 优化聚合
}
```

### 7.3 缺失的大数据防护措施

**当前实现存在的风险：**

1. **无真正的分批查询**：虽有LIMIT 500，但无法通过多次请求获取完整数据集（导出接口不支持分页参数的实际使用）

2. **无限流/速率限制**：
   - 无API调用频率限制
   - 无并发导出数量限制
   - 无单用户导出频率控制

3. **无内存保护**：
   - 所有查询结果一次性加载到内存
   - ZIP打包在内存中完成
   - 无流式查询/流式写入

4. **无超时保护**：
   - 查询超时依赖数据库配置
   - HTTP超时依赖服务器配置
   - 无主动超时中断机制

5. **无查询复杂度检查**：
   - 不过滤条件数量
   - 不检查日期范围大小
   - 大数据范围可能导致慢查询

### 7.4 大数据导出风险评估

| 数据量级 | 预期响应时间 | 内存占用 | 风险等级 |
|---------|-------------|---------|---------|
| < 1万条 | < 2s | < 10MB | 低 |
| 1-10万条 | 2-10s | 10-100MB | 中 |
| > 10万条 | > 10s | > 100MB | 高 |

**高风险场景：**
- 大时间范围（如1年）+ 高流量网站
- 细粒度事件类型多（自定义事件）
- 多维度组合过滤

---

## 八、未授权字段的拦截层级

### 8.1 权限控制层次结构

```
┌─────────────────────────────────────────┐
│  层级1：API网关层 (Next.js路由)         │
│  - HTTP方法限制                         │
│  - 路由参数验证 (Zod Schema)            │
└─────────────────────┬───────────────────┘
                      │
┌─────────────────────▼───────────────────┐
│  层级2：认证层 (parseRequest)           │
│  - Token解析与校验                      │
│  - 共享Token上下文校验                  │
│  - 返回 401 Unauthorized                │
└─────────────────────┬───────────────────┘
                      │
┌─────────────────────▼───────────────────┐
│  层级3：资源授权层 (canViewWebsite)     │
│  ┌─ 3.1 管理员直接放行                  │
│  ├─ 3.2 共享Token：资源ID匹配           │
│  └─ 3.3 实体归属：个人/团队校验         │
│  - 返回 401 Unauthorized                │
└─────────────────────┬───────────────────┘
                      │
┌─────────────────────▼───────────────────┐
│  层级4：参数白名单层 (getRequestFilters) │
│  - 只保留 FILTER_COLUMNS 中定义的字段   │
│  - 未知字段自动丢弃                     │
└─────────────────────┬───────────────────┘
                      │
┌─────────────────────▼───────────────────┐
│  层级5：查询构建层 (parseFilters)       │
│  - 过滤条件映射到数据库列名             │
│  - 自动判断是否需要JOIN会话表           │
│  - 防止SQL注入（参数化查询）            │
└─────────────────────┬───────────────────┘
                      │
┌─────────────────────▼───────────────────┐
│  层级6：数据范围层 (setWebsiteDate)     │
│  - 云模式订阅用户数据范围限制           │
│  - 网站重置日期限制                     │
└─────────────────────────────────────────┘
```

### 8.2 各层级拦截详解

#### 层级1：API网关层 - 参数Schema验证

**拦截位置：** `route.ts:13-17` + `schema.ts`

**实现：**
```typescript
const schema = withDateRange({
  ...pagingParams,
});

const { auth, query, error } = await parseRequest(request, schema);
```

**拦截内容：**
- 非法日期格式 → 400 Bad Request
- 无效时区 → 400 Bad Request
- 无效时间单位 → 400 Bad Request
- 缺少必要日期参数 → 400 Bad Request

#### 层级2：认证层 - 身份验证

**拦截位置：** `request.ts:44-50` → `auth.ts:17-60`

**实现：**
```typescript
if (!options?.skipAuth && !error) {
  auth = await checkAuth(request);
  if (!auth) {
    error = () => unauthorized();
  }
}
```

**拦截内容：**
- 缺少Authorization头 → 401
- 无效/过期Token → 401
- 共享Token缺少上下文头 → 401

#### 层级3：资源授权层 - 核心权限校验（3个子层级）

**拦截位置：** `route.ts:25-27` → `website.ts:7-40`

**实现：**
```typescript
if (!(await canViewWebsite(auth, websiteId))) {
  return unauthorized();
}
```

**拦截逻辑（按优先级顺序）：**

| 子层级 | 校验类型 | 判断条件 | 结果 |
|-------|---------|---------|------|
| 3.1 | 管理员放行 | `user.isAdmin === true` | ✅ 放行 |
| 3.2 | 共享Token | `shareToken.websiteId === websiteId` 或ID在数组中 | ✅ 放行 |
| 3.3 | 实体归属 - 个人 | `user.id === entity.userId` | ✅ 放行 |
| 3.3 | 实体归属 - 团队 | `getTeamUser(teamId, userId)` 存在 | ✅ 放行 |
| - | 其他情况 | 以上都不满足 | ❌ 401 |

**对字段可见性的影响**：
- 通过校验的用户：可见所有7个CSV文件的 `x, y` 字段
- 未通过校验的用户：返回401，无任何数据

#### 层级4：参数白名单层 - 过滤字段校验

**拦截位置：** `request.ts:79-90`

**实现：**
```typescript
export function getRequestFilters(query: Record<string, any>) {
  const result: Record<string, any> = {};
  for (const key of Object.keys(query)) {
    const baseName = key.replace(/\d+$/, '');
    if (baseName in FILTER_COLUMNS) {  // 白名单检查
      result[key] = query[key];
    }
  }
  return result;
}
```

**白名单 (`constants.ts:72-97`)：**
- 仅 `FILTER_COLUMNS` 中定义的26个字段可作为过滤条件
- 未知字段被静默丢弃，不进入后续处理

**对字段可见性的影响**：
- 防止非法字段名作为过滤条件注入
- 不影响导出的实际字段（导出字段由查询的 `type` 参数决定）

#### 层级5：查询构建层 - 列名映射与注入防护

**拦截位置：** `prisma.ts:74-106` / `clickhouse.ts:73-99`

**实现：**
```typescript
function mapFilter(column, operator, name, type, paramName) {
  const table = SESSION_COLUMNS.includes(name) ? 'session' : 'website_event';
  
  switch (operator) {
    case OPERATORS.equals:
      return `${table}.${column} = ANY(${value})`;
    case OPERATORS.contains:
      return `${table}.${column} ilike ${value}`;
    // ... 其他操作符
  }
}
```

**安全机制：**
1. **列名白名单**：`column` 必须来自 `FILTER_COLUMNS` 映射
2. **表名自动判断**：根据字段类型自动选择 `website_event` 或 `session` 表
3. **参数化查询**：所有用户输入通过 `{{param}}` 占位符传递，不拼接SQL
4. **操作符白名单**：仅允许预定义的15种操作符 (`OPERATORS`)

**对字段可见性的影响**：
- 确保SQL查询的列名是合法的数据库字段
- 防止SQL注入攻击
- 不直接影响导出字段，只影响过滤条件

#### 层级6：数据范围层 - 时间与业务限制

**拦截位置：** `request.ts:92-109`

**实现：**
```typescript
export async function setWebsiteDate(websiteId, data) {
  // 云模式非订阅用户：限制为最近6个月
  if (cloudMode && website && !website.teamId) {
    const account = await fetchAccount(website.userId);
    if (!account?.hasSubscription) {
      data.startDate = maxDate(data.startDate, startOfMonth(subMonths(new Date(), 6)));
    }
  }
  
  // 网站重置日期：不早于重置日期
  if (website?.resetAt) {
    data.startDate = maxDate(data.startDate, new Date(website?.resetAt));
  }
  
  return data;
}
```

**对字段可见性的影响**：
- 影响导出数据的**时间范围**，不影响**字段数量**
- 非订阅用户导出的数据不超过最近6个月
- 字段仍然是 `x, y` 两列

### 8.3 关键决策点：为什么在这个层级拦截？

| 决策点 | 原因分析 |
|-------|---------|
| 参数验证放在最外层 | 尽早拒绝非法请求，减少后续资源消耗 |
| 认证在授权之前 | 先确认"你是谁"，再判断"你能做什么" |
| 管理员校验优先级最高 | 减少管理员的查询开销，直接放行 |
| 共享Token在实体归属之前 | 共享用户无需查询实体归属，提高效率 |
| 资源授权在查询之前 | 无权限则不执行任何数据库操作 |
| 字段白名单在查询构建之前 | 过滤无效参数，降低注入风险 |
| 列名映射在SQL构建时 | 统一字段名到列名的转换逻辑，避免重复 |
| 数据范围在最后 | 基于业务规则的动态限制，依赖前面的认证结果 |

---

## 九、关键决策点总结

### 9.1 架构决策

| 决策项 | 选择 | 理由 |
|-------|------|------|
| 导出方式 | 同步 | 实现简单，满足当前数据规模需求 |
| 文件格式 | ZIP + CSV | 通用格式，无需额外依赖，兼容性好 |
| 编码方式 | Base64 | 避免二进制传输问题，简化响应处理 |
| 数据库支持 | PostgreSQL + ClickHouse | 满足不同部署规模的性能需求 |
| 并行查询 | Promise.all | 充分利用IO等待时间，提升响应速度 |

### 9.2 字段与权限决策

| 决策项 | 选择 | 理由 |
|-------|------|------|
| 导出字段集 | 固定7类查询，硬编码type参数 | 简化实现，降低注入风险，导出数据敏感度低 |
| 字段级权限 | 无动态字段裁剪 | 所有授权用户看到相同字段（x, y），通过资源级权限控制访问 |
| 管理员放行 | 优先级最高 | 减少管理员查询开销 |
| 共享令牌校验 | OR关系，第二优先级 | 支持匿名访问场景 |
| 实体归属校验 | 最后兜底 | 确保个人/团队数据隔离 |

### 9.3 安全决策

| 决策项 | 选择 | 理由 |
|-------|------|------|
| 权限层级 | 6层纵深防御 | 最小权限原则，每层只负责单一职责 |
| 字段级权限 | 固定字段集 | 导出数据为聚合指标，敏感度低 |
| SQL注入防护 | 参数化查询 + 白名单 | 多重防护，杜绝拼接风险 |
| 数据范围限制 | 时间窗口裁剪 | 云模式商业规则强制 |

### 9.4 性能决策

| 决策项 | 选择 | 理由 |
|-------|------|------|
| 查询限制 | LIMIT 500 | 防止单个查询返回过多数据 |
| 聚合优化 | 预聚合表自动选择 | ClickHouse场景下性能提升明显 |
| 内存管理 | 全内存处理 | 当前数据规模下可接受，简化实现 |

---

## 十、代码文件索引

| 功能模块 | 文件路径 | 关键行号 |
|---------|---------|---------|
| 前端导出按钮 | `src/components/input/ExportButton.tsx` | 9-64 |
| 导出API路由 | `src/app/api/websites/[websiteId]/export/route.ts` | 9-62 |
| 对比：Metrics路由 | `src/app/api/websites/[websiteId]/metrics/route.ts` | 14-65 |
| 请求解析 | `src/lib/request.ts` | 12-178 |
| 权限校验（3个层级） | `src/permissions/website.ts` | 7-40 |
| 身份认证 | `src/lib/auth.ts` | 17-87 |
| 实体查询 | `src/lib/entity.ts` | 4-14 |
| 参数Schema | `src/lib/schema.ts` | 1-84 |
| 角色定义 | `src/lib/constants.ts` | 72-97, 164-210 |
| PostgreSQL查询 | `src/lib/prisma.ts` | 74-253 |
| ClickHouse查询 | `src/lib/clickhouse.ts` | 73-236 |
| 事件指标（events.csv） | `src/queries/sql/events/getEventMetrics.ts` | 15-96 |
| 页面指标（pages/referrers.csv） | `src/queries/sql/pageviews/getPageviewMetrics.ts` | 15-197 |
| 会话指标（browsers/os等.csv） | `src/queries/sql/sessions/getSessionMetrics.ts` | 15-137 |
| 过滤器解析 | `src/lib/params.ts` | 42-90 |
| 响应格式 | `src/lib/response.ts` | 1-58 |
| 数据库路由 | `src/lib/db.ts` | 22-36 |
| 前端API封装 | `src/components/hooks/useApi.ts` | 20-75 |
| HTTP请求方法 | `src/lib/fetch.ts` | 1-58 |
| URL构建 | `src/lib/url.ts` | 1-16 |
| 日期参数Hook | `src/components/hooks/useDateParameters.ts` | 1-18 |
| 过滤参数Hook | `src/components/hooks/useFilterParameters.ts` | 1-28 |
| 导航Hook | `src/components/hooks/useNavigation.ts` | 1-58 |
| 类型定义 | `src/lib/types.ts` | 56-115 |

---

## 十一、修订说明

### 第一次修订

1. **events.csv 列结构**：澄清了接口定义中的 `t` 字段实际未被查询，所有7个CSV文件都只有 `x` 和 `y` 两列

2. **各导出文件的字段差异**：详细对比了7个文件的数据源、type参数、映射列和统计口径，指出 `y` 列在不同文件中的计数方式不同

3. **完整的权限拦截流程**：将管理员放行、共享令牌校验、实体归属校验串成完整的OR关系流程图，明确了各环节的优先级和代码位置

4. **权限对字段可见性的实际影响**：明确指出三种权限校验都是资源级权限，而非字段级权限，一旦通过校验所有用户看到的字段完全相同，差异只在于"能不能导出"而非"能导出哪些字段"

### 第三次修订

1. **`...searchParams` 展开行为修正**：明确指出 Next.js 的 `useSearchParams()` 返回 `URLSearchParams` 对象，使用展开运算符 `...` 时返回空对象 `{}`，不贡献任何参数。所有实际生效的参数都来自 `...date` 和 `...filters`

2. **`...searchParams` 与 `useFilterParameters` 的作用范围区分**：
   - `...searchParams`：无效，展开后为空
   - `useFilterParameters()`：内部通过 `useNavigation().query` → `Object.fromEntries(searchParams)` 正确转换，是所有过滤条件和分页参数的实际来源

3. **过滤条件透传的完整证据链**：重新整理了从浏览器URL → `useSearchParams` → `useNavigation` → `useFilterParameters` → `ExportButton` → `buildPath` → `parseRequest` → Zod Schema → `getQueryFilters` → `get*Metrics` 共8个节点的完整证据链，每个节点都标注了代码位置和值的变化

4. **以 `browser=eq.Chrome` 为例的逐节点追踪**：详细展示了过滤条件在每个节点的存在状态，明确指出在 Zod Schema 验证节点被丢弃的具体原因（Schema未声明 `filterParams`）

5. **代码冗余问题识别**：指出了三处代码冗余：
   - `...searchParams` 展开无效
   - `useFilterParameters` 提取的过滤条件最终被 Zod 丢弃
   - `pagingParams` 在 Schema 中声明但最终不生效

### 第四次修订

1. **无后缀键与后缀键的差异化处理路径**：深入分析了 `parseRequest` 中 Schema 过滤 + 数字后缀回补的精确机制，明确指出：
   - **无后缀键（如 `browser`）**：被 Schema 过滤后，因无数字后缀不触发 `/\d+$/` 回补，最终完全丢失
   - **带后缀键（如 `browser1`）**：被 Schema 过滤后，因有数字后缀触发 `/\d+$/` 回补，成功保留在 query 中

2. **导出接口过滤条件生效范围的重要发现**：
   - ❌ **不支持单个过滤条件**：无后缀键会丢失
   - ✅ **只支持多个过滤条件**：带后缀键会被回补保留
   - 这是一个设计上的不一致性，用户可能会感到困惑

3. **完整的8节点差异化证据链**：以 `browser`（无后缀）和 `browser1`（带后缀）为例，对比追踪了从浏览器URL → SQL查询构建的8个节点，详细列出每个节点两种键的不同命运：
   - 节点5（Zod Schema验证）：都被删除
   - 节点6（数字后缀回补）：分水岭，`browser` 不回补，`browser1` 回补
   - 节点7-8：`browser` 彻底丢失，`browser1` 进入SQL

4. **paramName/name/column 三者的区别**：明确了在 `filtersObjectToArray` 和 SQL 构建中的三个关键变量的作用：
   - `paramName`：保留原始带后缀键名（如 `browser1`），用于参数绑定避免命名冲突
   - `name`：去掉后缀的基础名称（如 `browser`），用于判断表归属
   - `column`：最终映射的数据库列名（如 `browser`）
