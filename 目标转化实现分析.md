# 目标转化（Goal Conversion）实现分析

## 一、系统架构概述

Umami 的目标转化系统采用 **"采集存储分离、查询时匹配"** 的架构设计：

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   采集层        │────▶│   存储层        │────▶│   查询层        │
│  (Tracker)      │     │  (Database)     │     │  (Reports)     │
│  - 原始事件采集 │     │  - WebsiteEvent │     │  - 目标匹配     │
│  - 无目标识别   │     │  - Report       │     │  - 数据汇总     │
│                 │     │  - Session      │     │  - 报表生成     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

---

## 二、采集层：事件采集与存储

### 2.1 前端事件采集 (`src/tracker/index.js`)

**核心职责**：采集原始事件，不做任何目标匹配

```javascript
// 事件类型
// - 页面浏览 (pageview): 自动触发，无 eventName
// - 自定义事件 (custom event): 通过 data-umami-event 或 umami.track() 触发

// 核心函数
const track = (name, data) => {
  if (typeof name === 'string') return send({ ...getPayload(), name, data });
  // ...
};

// 发送到后端
const send = async (payload, type = 'event') => {
  return fetch(endpoint, {
    method: 'POST',
    body: JSON.stringify({ type, payload }),
    // ...
  });
};
```

**关键特性**：
- 采集层完全不知道"目标"的存在
- 只负责收集原始事件数据（url、eventName、eventData 等）
- 支持 `path` 类型（页面访问）和 `event` 类型（自定义事件）

### 2.2 事件接收 API (`src/app/api/send/route.ts`)

**核心职责**：验证、解析并存储事件

```typescript
// 事件类型判断 (route.ts:217-223)
const eventType = linkId
  ? EVENT_TYPE.linkEvent
  : pixelId
    ? EVENT_TYPE.pixelEvent
    : name
      ? EVENT_TYPE.customEvent  // 有 name = 自定义事件
      : EVENT_TYPE.pageView;    // 无 name = 页面浏览

// 存储事件
await saveEvent({
  websiteId: sourceId,
  sessionId,
  visitId,
  eventType,
  urlPath,
  eventName: name,  // 事件名称（用于目标匹配）
  eventData: data,  // 事件数据（用于漏斗过滤）
  // ... 其他字段
});
```

**存储位置**：
- 主数据：`website_event` 表 (`prisma/schema.prisma:99-150`)
- 事件属性：`event_data` 表（当 eventData 存在时）

### 2.3 事件存储 (`src/queries/sql/events/saveEvent.ts`)

**双数据库支持**：

```typescript
export async function saveEvent(args: SaveEventArgs) {
  return runQuery({
    [PRISMA]: () => relationalQuery(args),      // PostgreSQL
    [CLICKHOUSE]: () => clickhouseQuery(args),  // ClickHouse
  });
}
```

**关键设计**：
- 事件存储时**不进行任何目标匹配**
- 只做数据规范化和持久化
- 目标匹配完全推迟到查询阶段

---

## 三、目标定义与数据模型

### 3.1 Report 表结构 (`prisma/schema.prisma:232-251`)

```prisma
model Report {
  id          String    @id() @map("report_id")
  userId      String    @map("user_id")
  websiteId   String    @map("website_id")
  type        String    @db.VarChar(50)       // 'goal', 'funnel', 'attribution' 等
  name        String    @db.VarChar(200)      // 目标名称
  description String    @db.VarChar(500)
  parameters  Json                             // 目标规则配置
  createdAt   DateTime?
  updatedAt   DateTime?
}
```

### 3.2 Goal 参数结构 (`src/lib/schema.ts:160-169`)

```typescript
export const goalReportSchema = z.object({
  type: z.literal('goal'),
  parameters: z.object({
    startDate: z.coerce.date(),
    endDate: z.coerce.date(),
    type: z.string(),      // 'path' | 'event'
    value: z.string(),     // 匹配值，支持通配符 *
  }),
});
```

**目标类型**：
- `path`: 匹配 `url_path` 字段（页面访问目标）
- `event`: 匹配 `event_name` 字段（自定义事件目标）

**通配符支持**：
- `*` 开头或结尾：使用 `LIKE` 匹配
- 例如：`/checkout/*` 匹配所有 checkout 下的页面

---

## 四、查询层：目标匹配与数据汇总

### 4.1 目标匹配核心逻辑 (`src/queries/sql/reports/getGoal.ts`)

**查询时动态匹配**：

```typescript
async function relationalQuery(
  websiteId: string,
  parameters: GoalParameters,
  filters: QueryFilters,
) {
  const { startDate, endDate, type, value } = parameters;
  
  // 根据目标类型选择匹配字段
  const eventType = type === 'path' ? EVENT_TYPE.pageView : EVENT_TYPE.customEvent;
  const column = type === 'path' ? 'url_path' : 'event_name';

  // 通配符处理
  let operator = '=';
  let paramValue = value;
  if (value.startsWith('*') || value.endsWith('*')) {
    operator = 'like';
    paramValue = value.replace(/^\*|\*$/g, '%');
  }

  // 生成过滤条件
  const { filterQuery, dateQuery, joinSessionQuery, cohortQuery, queryParams } = 
    parseFilters({
      ...filters,
      websiteId,
      value: paramValue,
      startDate,
      endDate,
      eventType,
    });

  // 核心 SQL：统计转化数和总访客数
  return rawQuery(
    `
    select count(distinct website_event.session_id) as num,  -- 转化数
    (
      select count(distinct website_event.session_id)
      from website_event
      where website_event.website_id = {{websiteId::uuid}}
        ${dateQuery}
        ${excludeEventTypeFilterQuery}
    ) as total  -- 总访客数
    from website_event
    where website_event.website_id = {{websiteId::uuid}}
      and ${column} ${operator} {{value}}  -- 目标匹配条件
      ${dateQuery}
      ${filterQuery}
    `,
    queryParams,
  ).then(results => results?.[0]);
}
```

**关键指标**：
- `num`: 完成目标的唯一访客数（`count(distinct session_id)`）
- `total`: 时间范围内的总访客数
- 转化率 = `num / total`

### 4.2 统一过滤解析器 (`src/lib/prisma.ts:232-253`)

**`parseFilters` 函数**是查询层的核心，所有报表类型共享：

```typescript
function parseFilters(filters: Record<string, any>, options?: QueryOptions) {
  const joinSession = Object.keys(filters).find(key => {
    const baseName = key.replace(/\d+$/, '');
    return ['referrer', ...SESSION_COLUMNS].includes(baseName);
  });

  return {
    // 自动判断是否需要 JOIN session 表
    joinSessionQuery: joinSession
      ? `inner join session on website_event.session_id = session.session_id`
      : '',
    // 日期范围过滤
    dateQuery: getDateQuery(filters),
    // 维度过滤（浏览器、国家、来源等）
    filterQuery: getFilterQuery(filters, options),
    // 查询参数（防止 SQL 注入）
    queryParams: getQueryParams(filters),
    // 群组/队列过滤
    cohortQuery: getCohortQuery(cohortFilters),
    // 排除跳出会话
    excludeBounceQuery: getExcludeBounceQuery(filters),
  };
}
```

**操作符映射** (`src/lib/prisma.ts:74-106`)：

| 操作符 | SQL 语法 |
|--------|----------|
| `eq` (equals) | `column = ANY([value])` |
| `neq` (not equals) | `column != ALL([value])` |
| `c` (contains) | `column ILIKE '%value%'` |
| `dnc` (does not contain) | `column NOT ILIKE '%value%'` |
| `re` (regex) | `column ~* 'value'` |
| `nre` (not regex) | `column !~* 'value'` |

### 4.3 参数格式转换 (`src/lib/params.ts`)

**URL 参数解析**：

```typescript
// 输入: { browser: 'eq.Chrome,Firefox' }
// 输出: { operator: 'eq', value: ['Chrome', 'Firefox'] }

export function parseFilterValue(param: any) {
  const regex = new RegExp(`^(${operatorValues})\\.(.*)$`);
  const [, operator, value] = param.match(regex) || [];
  
  if (operator === 'eq' || operator === 'neq') {
    return { operator, value: value.split(',') };
  }
  return { operator: operator || 'eq', value: value ?? param };
}
```

---

## 五、多目标重叠时的归因优先级

### 5.1 设计模式：独立计数（Independent Counting）

**Umami 不采用"首次触达"或"末次触达"的归因模型，而是采用**独立计数**模式：

```
事件 E (访问 /checkout/complete)
    │
    ├─▶ 目标 A: /checkout/* → 命中，计数 +1
    ├─▶ 目标 B: /checkout/complete → 命中，计数 +1
    └─▶ 目标 C: purchase → 未命中
```

**关键特性**：
- 每个目标独立执行查询，互不影响
- 一个事件可以同时被多个目标命中
- **不存在优先级竞争**
- 归因在目标维度独立计算

### 5.2 代码依据

在 `getGoal.ts` 中，每个目标查询都是独立的 SQL：

```typescript
// 目标 A 查询
select count(distinct session_id) 
from website_event 
where url_path like '/checkout/%'

// 目标 B 查询  
select count(distinct session_id) 
from website_event 
where url_path = '/checkout/complete'
```

**前端展示** (`src/app/(main)/websites/[websiteId]/(reports)/goals/GoalsPage.tsx`)：

```typescript
// 每个 Goal 组件独立调用 useResultQuery
{data.data.map((report: any) => (
  <Panel key={report.id}>
    <Goal {...report} startDate={startDate} endDate={endDate} />
  </Panel>
))}
```

### 5.3 归因报告的特殊处理 (`src/queries/sql/reports/getAttribution.ts`)

**归因报告**是唯一涉及归因模型的地方，支持：

- `first-click`（首次点击）：使用 `min(created_at)`
- `last-click`（末次点击）：使用 `max(created_at)`

```typescript
function getModelQuery(model: string) {
  return model === 'first-click'
    ? `select e.session_id, min(we.created_at) created_at from events e ...`
    : `select e.session_id, max(we.created_at) created_at from events e ...`;
}
```

**注意**：这是**渠道归因**（将转化归因到哪个来源渠道），不是目标优先级。

---

## 六、报表请求的查询逻辑复用

### 6.1 整体复用架构

所有报表类型共享相同的五层架构：

```
┌─────────────────────────────────────────────────────┐
│  前端层: useResultQuery / useReportsQuery           │
│  (React Query hooks, 统一缓存和请求管理)            │
├─────────────────────────────────────────────────────┤
│  API 路由层: /api/reports/{type}                    │
│  (统一鉴权、参数解析、日期处理)                      │
├─────────────────────────────────────────────────────┤
│  查询函数层: getGoal / getFunnel / getAttribution   │
│  (业务逻辑封装, 双数据库支持)                       │
├─────────────────────────────────────────────────────┤
│  过滤解析层: parseFilters                           │
│  (统一 SQL 条件生成, 防注入)                        │
├─────────────────────────────────────────────────────┤
│  数据库抽象层: runQuery                             │
│  (PostgreSQL / ClickHouse 自动切换)                 │
└─────────────────────────────────────────────────────┘
```

### 6.2 API 路由层复用

**所有报表 API 路由采用完全相同的模式**：

```typescript
// goal/route.ts
export async function POST(request: Request) {
  const { auth, body, error } = await parseRequest(request, reportResultSchema);
  const parameters = await setWebsiteDate(websiteId, body.parameters);
  const filters = await getQueryFilters(body.filters, websiteId);
  const data = await getGoal(websiteId, parameters, filters);
  return json(data);
}

// funnel/route.ts  
export async function POST(request: Request) {
  const { auth, body, error } = await parseRequest(request, reportResultSchema);
  const parameters = await setWebsiteDate(websiteId, body.parameters);
  const filters = await getQueryFilters(body.filters, websiteId);
  const data = await getFunnel(websiteId, parameters, filters);  // 只有这里不同
  return json(data);
}
```

**共享组件**：
- `parseRequest`: 统一的请求解析和 Zod Schema 验证
- `setWebsiteDate`: 统一的日期范围处理（重置日期、订阅限制）
- `getQueryFilters`: 统一的过滤参数解析（含 segment/cohort 展开）

### 6.3 查询函数层复用

**所有报表查询函数遵循相同的双数据库模式**：

```typescript
// getGoal.ts
export async function getGoal(...args) {
  return runQuery({
    [PRISMA]: () => relationalQuery(...args),
    [CLICKHOUSE]: () => clickhouseQuery(...args),
  });
}

// getFunnel.ts
export async function getFunnel(...args) {
  return runQuery({
    [PRISMA]: () => relationalQuery(...args),
    [CLICKHOUSE]: () => clickhouseQuery(...args),
  });
}

// getAttribution.ts
export async function getAttribution(...args) {
  return runQuery({
    [PRISMA]: () => relationalQuery(...args),
    [CLICKHOUSE]: () => clickhouseQuery(...args),
  });
}
```

### 6.4 前端 Hooks 复用

**`useResultQuery` (`src/components/hooks/queries/useResultQuery.ts`)** 是所有报表的统一前端入口：

```typescript
export function useResultQuery<T = any>(
  type: string,           // 'goal', 'funnel', 'attribution' 等
  params?: Record<string, any>,
  options?: ReactQueryOptions<T>,
) {
  const { websiteId, ...parameters } = params;
  const { startDate, endDate, timezone, unit } = useDateParameters();
  const filters = useFilterParameters();

  return useQuery<T>({
    queryKey: ['reports', { type, websiteId, startDate, ...filters }],
    queryFn: () =>
      post(`/reports/${type}`, {
        websiteId,
        type,
        filters,
        parameters: { startDate, endDate, timezone, unit, ...parameters },
      }),
    enabled: !!type,
    ...options,
  });
}
```

**复用点**：
- 自动从 URL 获取日期参数 (`useDateParameters`)
- 自动从 URL 获取过滤参数 (`useFilterParameters`)
- 统一的 React Query 缓存 key 生成
- 统一的请求格式

### 6.5 数据库抽象层复用

**`runQuery` (`src/lib/db.ts:22-36`)** 实现透明的数据库切换：

```typescript
export async function runQuery(queries: any) {
  if (process.env.CLICKHOUSE_URL) {
    if (queries[KAFKA]) {
      return queries[KAFKA]();
    }
    return queries[CLICKHOUSE]();
  }

  const db = getDatabaseType();
  if (db === POSTGRESQL) {
    return queries[PRISMA]();
  }
}
```

**优先级**：Kafka > ClickHouse > PostgreSQL

---

## 七、完整数据流示例

### 7.1 目标创建流程

```
用户在 UI 创建目标
  ↓
POST /api/reports (type: 'goal', parameters: { type: 'path', value: '/checkout/*' })
  ↓
存入 report 表
  ├─ type: 'goal'
  ├─ parameters: { type: 'path', value: '/checkout/*' }
  └─ ...
```

### 7.2 目标转化追踪流程

```
用户访问 /checkout/complete
  ↓
Tracker 触发 track()
  ↓
POST /api/send { type: 'event', payload: { url: '/checkout/complete' } }
  ↓
保存到 website_event 表
  ├─ url_path: '/checkout/complete'
  ├─ event_type: 1 (pageView)
  └─ ... (无任何目标标记)
```

### 7.3 报表查询流程

```
用户访问目标报表页面
  ↓
GET /api/reports?websiteId=xxx&type=goal → 获取目标定义列表
  ↓
每个 Goal 组件调用 useResultQuery('goal', { ... })
  ↓
POST /api/reports/goal
  {
    websiteId: 'xxx',
    filters: { startAt: 123, endAt: 456 },
    parameters: { type: 'path', value: '/checkout/*' }
  }
  ↓
getGoal() 调用 parseFilters() 生成 SQL
  ↓
SQL 查询:
  SELECT count(DISTINCT session_id) as num,
         (SELECT count(DISTINCT session_id) FROM website_event WHERE ...) as total
  FROM website_event
  WHERE website_id = 'xxx'
    AND url_path LIKE '/checkout/%'  ← 目标匹配在这里发生
    AND created_at BETWEEN ...
  ↓
返回 { num: 150, total: 1000 } → 转化率 15%
```

---

## 八、关键设计决策与权衡

### 8.1 查询时匹配 vs 写入时匹配

| 维度 | 查询时匹配 (Umami 采用) | 写入时匹配 |
|------|------------------------|------------|
| 写入性能 | ✅ 高，无额外计算 | ❌ 低，每个事件需匹配所有目标 |
| 查询性能 | ❌ 每次查询需扫描数据 | ✅ 预聚合，查询快 |
| 灵活性 | ✅ 可修改历史目标规则 | ❌ 修改规则不影响历史数据 |
| 存储成本 | ✅ 无需额外存储 | ❌ 需存储目标关联关系 |
| 实时性 | ✅ 规则立即生效 | ❌ 需等待新数据 |

### 8.2 独立计数 vs 归因模型

| 维度 | 独立计数 (Umami 采用) | 归因模型 |
|------|------------------------|----------|
| 实现复杂度 | ✅ 简单 | ❌ 复杂 |
| 数据准确性 | ✅ 每个目标真实数据 | ⚠️ 依赖归因规则 |
| 用户理解成本 | ✅ 直观易懂 | ❌ 需要解释归因逻辑 |
| 多目标重叠 | ✅ 全部统计 | ⚠️ 可能丢失信息 |
| 适用场景 | 简单目标追踪 | 广告渠道分析 |

---

## 九、核心代码文件索引

| 层级 | 文件路径 | 核心职责 |
|------|----------|----------|
| 采集层 | `src/tracker/index.js` | 前端事件采集脚本 |
| 采集层 | `src/app/api/send/route.ts` | 事件接收 API |
| 采集层 | `src/queries/sql/events/saveEvent.ts` | 事件持久化 |
| 数据模型 | `prisma/schema.prisma` | Report、WebsiteEvent 表定义 |
| 查询层 | `src/queries/sql/reports/getGoal.ts` | 目标转化查询 |
| 查询层 | `src/queries/sql/reports/getFunnel.ts` | 漏斗分析查询 |
| 查询层 | `src/queries/sql/reports/getAttribution.ts` | 渠道归因查询 |
| 过滤解析 | `src/lib/prisma.ts` | PostgreSQL 过滤解析器 |
| 过滤解析 | `src/lib/clickhouse.ts` | ClickHouse 过滤解析器 |
| 参数处理 | `src/lib/params.ts` | 过滤器参数格式转换 |
| 参数处理 | `src/lib/request.ts` | HTTP 请求参数解析 |
| 数据库抽象 | `src/lib/db.ts` | 多数据库路由 |
| Schema | `src/lib/schema.ts` | 报表参数 Zod Schema |
| 前端 Hooks | `src/components/hooks/queries/useResultQuery.ts` | 报表查询 Hook |
| 前端 Hooks | `src/components/hooks/queries/useReportsQuery.ts` | 报表列表 Hook |
| UI 组件 | `src/app/(main)/websites/[websiteId]/(reports)/goals/Goal.tsx` | 目标卡片组件 |
| UI 组件 | `src/app/(main)/websites/[websiteId]/(reports)/goals/GoalsPage.tsx` | 目标报表页面 |
| API 路由 | `src/app/api/reports/goal/route.ts` | 目标报表 API |
| API 路由 | `src/app/api/reports/route.ts` | 报表 CRUD API |

---

## 十、总结

Umami 的目标转化系统采用了 **"采集存储分离、查询时匹配、独立计数、高度复用"** 的设计理念：

1. **采集层** 只负责原始事件收集，不做任何目标识别，保证了高性能和灵活性
2. **查询层** 通过统一的 `parseFilters` 解析器动态生成 SQL，在查询时完成目标匹配
3. **多目标归因** 采用独立计数模式，一个事件可被多个目标同时命中，不存在优先级竞争
4. **报表复用** 通过五层架构（前端 Hooks → API 路由 → 查询函数 → 过滤解析 → 数据库抽象）实现了高度的代码复用
5. **双数据库支持** 通过 `runQuery` 抽象层透明支持 PostgreSQL 和 ClickHouse

这种设计使得目标规则的修改可以立即生效，历史数据也能按照新规则重新计算，非常适合分析场景的灵活性需求。
